apiVersion: apps/v1
kind: Deployment
metadata:
  name: getuser-app
  labels:
    app: getuser
spec:
  replicas: 2
  selector:
    matchLabels:
      app: getuser
  template:
    metadata:
      labels:
        app: getuser
    spec:
      initContainers:
        - name: config-check
          image: busybox:1
          command: ["sh", "-c", "env | grep DB_HOST"]
          env:
            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: pyapp-config
                  key: dbhost
        - name: mysql-test
          image: busybox:1
          command:
            [
              "sh",
              "-c",
              "until nslookup mysqldb.default.svc.cluster.local ; do echo Waiting for mysqldb; sleep 2;done",
            ]
      containers:
        - name: getuser
          image: adesh0303/eks-app:getuser-base
          env:
            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: pyapp-config
                  key: dbhost
            - name: DB_PASS
              valueFrom:
                configMapKeyRef:
                  name: pyapp-config
                  key: dbpass
            - name: DB_USER
              valueFrom:
                configMapKeyRef:
                  name: pyapp-config
                  key: dbuser
            - name: DB_PORT
              valueFrom:
                configMapKeyRef:
                  name: pyapp-config
                  key: dbport
            - name: DB_NAME
              valueFrom:
                configMapKeyRef:
                  name: pyapp-config
                  key: dbname
      tolerations:
        - key: "userapp"
          operator: "Equal"
          effect: "NoSchedule"
          value: "critical"
