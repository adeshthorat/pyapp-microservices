kind: Service
apiVersion: v1
metadata:
  name: frontend-svc
spec:
  selector:
    app: frontend
  type: ClusterIP  # Changed from NodePort - traffic via Istio Gateway
  ports:
    - name: frontend-port
      port: 80
      targetPort: 80
