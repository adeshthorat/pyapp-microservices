apiVersion: v1
kind: ConfigMap
metadata:
  name: pyapp-config
  labels:
    config: appspecs
data:
  # Non-sensitive configuration only
  databasename: "userdb"
  username: "myuser"
  dbhost: "mysqldb"
  dbport: "3306"
  dbname: "userdb"
  dbuser: "myuser"
  # Passwords moved to secrets.yaml for security
