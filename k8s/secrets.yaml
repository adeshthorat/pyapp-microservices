apiVersion: v1
kind: Secret
metadata:
  name: pyapp-secrets
  labels:
    app: pyapp
type: Opaque
stringData:
  # Database credentials - these will be base64 encoded automatically
  rootpass: "root"
  userpass: "mypass"
---
# Optional: For production, use ExternalSecret with AWS Secrets Manager, Vault, etc.
# Uncomment and configure if using external secrets management
# apiVersion: external-secrets.io/v1beta1
# kind: ExternalSecret
# metadata:
#   name: pyapp-secrets
# spec:
#   refreshInterval: 1h
#   secretStoreRef:
#     name: aws-secrets-manager
#     kind: SecretStore
#   target:
#     name: pyapp-secrets
#     creationPolicy: Owner
#   data:
#     - secretKey: rootpass
#       remoteRef:
#         key: pyapp/mysql/root-password
#     - secretKey: userpass
#       remoteRef:
#         key: pyapp/mysql/user-password
